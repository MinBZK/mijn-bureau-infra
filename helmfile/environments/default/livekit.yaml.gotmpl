livekit:
  # livekit/meet API credentials + URL. The meet app derives these from
  # application.livekit.keys + global.* when unset; override only to point at
  # an external livekit.
  meet:
    apiKey: ~
    apiSecret: ~
    apiURL: ~
