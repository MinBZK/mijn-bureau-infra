ai:
  # AI / LLM endpoint. Defaults to the in-cluster ollama;
  # override per environment to point at an external LLM.
  llm:
    model: ~
    endpoint:
      host: ~
      port: ~
      path: "v1"
      isSsl: true
      isInternal: false
    apiKey: ~
